use std::path::PathBuf;
use crate::sniffers::SniffResults;

pub struct JPAGenerator;

impl JPAGenerator {
    fn generate_table_files(&self, _sniff_results: SniffResults, _target_path: PathBuf) {
        todo!()
    }
}
