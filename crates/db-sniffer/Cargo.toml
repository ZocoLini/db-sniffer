[package]
name = "db-sniffer"
version = "0.1.0"
edition = "2021"

[dependencies]
odbc-api = { version = "10.0.0" }
sqlx = { version = "0.8.2", features = ["all-databases", "runtime-tokio"]}
regex = { version = "1.11.1" }